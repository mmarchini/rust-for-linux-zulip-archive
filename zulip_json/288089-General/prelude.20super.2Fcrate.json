[
    {
        "content": "<p>Looking at the prelude doc right now (preventing the inlining of items to make it easier to read). And wonder if it would be nicer to use <code>crate</code> instead of <code>super</code> for import in the kernel crate. IMHO that makes it also a bit nicer to read, but not sure what the ideal way would be.</p>",
        "id": 315763598,
        "sender_full_name": "Fiona Behrens",
        "timestamp": 1671007896
    }
]