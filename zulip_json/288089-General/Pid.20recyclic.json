[
    {
        "content": "<p>Does taking a refcount on a <code>struct pid</code> prevent the numeric pid from getting recycled?</p>",
        "id": 441773498,
        "sender_full_name": "Alice Ryhl",
        "timestamp": 1717179413
    },
    {
        "content": "<p>Afaik (pls double check :) ), <code>struct pid</code>'s purpose is to decouple the kernel's notion to identify a process from the numeric values reported to user space (a process could equivalently be uniquely identified by its <code>task_struct</code>but keeping that around after exit wastes too much memory). As such, the numeric values (in possilby many namespaces) will be freed on <code>exit()</code>if no tasks are using it anymore (see call paths leading to <code>free_pid</code>, which is where numeric values are given back to <code>ns-&gt;idr</code>s), i.e., a ref to <code>struct pid</code>will not prevent their recycling. I guess another way to see that would be to note that <code>put_pid</code> has no code to mark the numeric values as reusable when the refcount drops to 0.</p>",
        "id": 441809084,
        "sender_full_name": "Valentin Obst",
        "timestamp": 1717190677
    }
]