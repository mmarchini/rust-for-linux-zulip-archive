[
    {
        "content": "<p>I played a bit with implementing attribute macros for conditional compilation based on kernel versions:</p>\n<div class=\"codehilite\" data-code-language=\"Rust\"><pre><span></span><code><span class=\"k\">use</span><span class=\"w\"> </span><span class=\"n\">kernel</span><span class=\"p\">::</span><span class=\"n\">macros</span><span class=\"p\">::</span><span class=\"o\">*</span><span class=\"p\">;</span>\n\n<span class=\"cp\">#[before(7, 0, 0)]</span>\n<span class=\"k\">fn</span><span class=\"w\"> </span><span class=\"nf\">call_kernel_fn</span><span class=\"p\">()</span><span class=\"w\"> </span><span class=\"p\">{</span>\n<span class=\"w\">    </span><span class=\"c1\">// implementation for kernels &lt; 7.0.0</span>\n<span class=\"p\">}</span>\n\n<span class=\"cp\">#[since(7, 0, 0)]</span>\n<span class=\"k\">fn</span><span class=\"w\"> </span><span class=\"nf\">call_kernel_fn</span><span class=\"p\">()</span><span class=\"w\"> </span><span class=\"p\">{</span>\n<span class=\"w\">    </span><span class=\"c1\">// implementation for kernels &gt;= 7.0.0</span>\n<span class=\"p\">}</span>\n</code></pre></div>\n<p>I think this can be useful for out-of-tree modules, as these can then target particular kernel versions' features, especially as more and more kernel APIs gets ported or wrapped with every new release.</p>\n<p>As this would be my first contribution here, before trying to send the patch I wanted to ask some questions and gather some preliminary feedback.</p>\n<p>First of all: do you also think there can be some value in this change?</p>\n<p>Next: at the moment everything lives in the <code>macros</code> crate: a couple of macros, an extra binding with related Makefile changes for <code>LINUX_VERSION_CODE</code> and some helpers to handle conversions between the <code>(major, patchlevel, sublevel)</code> and the <code>u32</code> kernel version format. </p>\n<p>For completeness I think these bindings and helpers should be exposed also in the <code>kernel</code> crate, so that a module can interact directly with the kernel version, but I'm unsure which approach would be best to follow:</p>\n<ul>\n<li>Duplicate the bindings and helpers in both the <code>kernel</code> and <code>macros</code> crates? Perhaps not, even if it's just a few LOCs</li>\n<li>Extract that logic into a separate crate shared between <code>kernel</code> and <code>macros</code>? Not sure if that would be useful for anything else in the future?</li>\n<li>Some other way to keep the bindings and helpers in the <code>kernel</code> crate and access them in the attribute macros in <code>macros</code>?</li>\n</ul>",
        "id": 579797534,
        "sender_full_name": "Gilberto Bertin",
        "timestamp": 1773694190
    },
    {
        "content": "<p>Upstream kernel won't accept code (and the associated maintenance burden) just to make out-of-tree modules easier. Note that this macro will be completely dead-code for the upstream.</p>",
        "id": 579842318,
        "sender_full_name": "Gary Guo",
        "timestamp": 1773715742
    },
    {
        "content": "<p>Makes sense, I thought this could be justified with the argument of  porting the <code>KERNEL_VERSION()</code>/ <code>LINUX_VERSION_CODE</code> API to Rust, as without a macro we can't really replicate the behavior of the  preprocessor eliminating unsupported code (and I don't think we can define attribute macros in out-of-tree modules?)</p>",
        "id": 579867012,
        "sender_full_name": "Gilberto Bertin",
        "timestamp": 1773732324
    },
    {
        "content": "<p>You can always carry those in out-of-tree modules. But apart from symbol-licensing, I cannot think of any APIs that were merged exclusively for out-of-tree modules. There is just a general dislike of those in the kernel community.</p>",
        "id": 579880486,
        "sender_full_name": "David Rheinsberg",
        "timestamp": 1773737088
    },
    {
        "content": "<blockquote>\n<p>There is just a general dislike of those in the kernel community.</p>\n</blockquote>\n<p>That's fair, and I can see the reasons why. </p>\n<blockquote>\n<p>You can always carry those in out-of-tree modules.</p>\n</blockquote>\n<p>I can always carry those symbols in out-of-tree modules, sure, but I cannot easily implement conditional compilation based on the kernel version without some bare support from the <code>macros</code> crate (I might be able to add some logic in the module's Makefile to turn the kernel version into a <code>--cfg kernel_ge_x_y_z</code> and pass it to <code>KBUILD_RUSTFLAGS</code> but that's not as clean as the initial proposal IMO).</p>\n<p>Anyway, thanks for the feedback, this was useful!</p>",
        "id": 579897960,
        "sender_full_name": "Gilberto Bertin",
        "timestamp": 1773741672
    }
]