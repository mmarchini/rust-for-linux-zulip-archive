[
    {
        "content": "<p>Hi all, I have been working on some virtio and V4L2 bindings recently.</p>\n<p>I don't think it's clear when to  wrap over a  pointer vs when to wrap over the bindgen struct directly, i.e.:</p>\n<div class=\"codehilite\" data-code-language=\"Rust\"><pre><span></span><code><span class=\"w\"> </span><span class=\"k\">struct</span> <span class=\"nc\">Wrapper</span><span class=\"p\">(</span><span class=\"o\">&lt;</span><span class=\"n\">some</span><span class=\"w\"> </span><span class=\"n\">pointer</span><span class=\"o\">&gt;</span><span class=\"p\">)</span>\n</code></pre></div>\n<p>vs</p>\n<div class=\"codehilite\" data-code-language=\"Rust\"><pre><span></span><code><span class=\"k\">struct</span> <span class=\"nc\">Wrapper</span><span class=\"p\">(</span><span class=\"n\">UnsafeCell</span><span class=\"o\">&lt;</span><span class=\"n\">bindings</span>::<span class=\"n\">some_struct</span><span class=\"o\">&gt;</span><span class=\"p\">)</span>\n</code></pre></div>\n<p>Both approaches are used in the kernel crate, so I wonder if we could have some guidelines on how to choose between these two and also on how to choose proper names for these.</p>\n<p>I'm asking because when prototyping a driver, at times I was passed in a pointer by some kernel callback, but there were instances where I found myself trying to instantiate the same object on the stack.</p>\n<p>I started naming the <code>struct Wrapper(&lt;some pointer&gt;)</code> variant as <code>struct FooRef(&lt;some_pointer&gt;)</code> to distinguish it from <code>struct Foo(UnsafeCell&lt;bindings::some_struct&gt;)</code>.</p>\n<p>Thoughts?</p>",
        "id": 341704813,
        "sender_full_name": "Daniel Almeida",
        "timestamp": 1678797202
    },
    {
        "content": "<p>I think overall the approach you are using is preferred, just note that if <code>some_struct</code> itself has a reference count, you may be able to  <code>impl AlwaysRefCounted for Foo</code>, and <code>FooRef</code> can be just <code>ARef&lt;Foo&gt;</code>.</p>",
        "id": 341809694,
        "sender_full_name": "Boqun Feng",
        "timestamp": 1678804001
    },
    {
        "content": "<p>I'm not involved with RFL, but here's my 2cents from other FFI work:</p>\n<div class=\"codehilite\" data-code-language=\"Rust\"><pre><span></span><code><span class=\"cp\">#[repr(transparent)]</span>\n<span class=\"k\">struct</span> <span class=\"nc\">SomeStruct</span><span class=\"p\">(</span><span class=\"n\">UnsafeCell</span><span class=\"o\">&lt;</span><span class=\"n\">bindings</span>::<span class=\"n\">some_struct</span><span class=\"o\">&gt;</span><span class=\"p\">)</span>\n</code></pre></div>\n<p>This is typically the pattern to use when you want a representation of a C structure in Rust. If you're often working with pointers, you typically will want something like:</p>\n<div class=\"codehilite\" data-code-language=\"Rust\"><pre><span></span><code><span class=\"k\">impl</span><span class=\"w\"> </span><span class=\"n\">SomeStruct</span><span class=\"p\">{</span>\n<span class=\"w\">    </span><span class=\"k\">pub</span><span class=\"p\">(</span><span class=\"k\">crate</span><span class=\"p\">)</span><span class=\"w\"> </span><span class=\"k\">unsafe</span><span class=\"w\"> </span><span class=\"k\">fn</span> <span class=\"nf\">from_raw_ptr</span><span class=\"o\">&lt;'</span><span class=\"na\">p</span><span class=\"o\">&gt;</span><span class=\"p\">(</span><span class=\"n\">ptr</span>: <span class=\"o\">*</span><span class=\"k\">mut</span><span class=\"w\"> </span><span class=\"n\">bindings</span>::<span class=\"n\">some_struct</span><span class=\"p\">)</span><span class=\"w\"> </span>-&gt; <span class=\"kp\">&amp;</span><span class=\"o\">'</span><span class=\"na\">p</span> <span class=\"nc\">Self</span><span class=\"w\"> </span><span class=\"p\">{</span>\n<span class=\"w\">        </span><span class=\"o\">&amp;*</span><span class=\"n\">ptr</span><span class=\"p\">.</span><span class=\"n\">cast</span><span class=\"p\">()</span>\n<span class=\"w\">    </span><span class=\"p\">}</span>\n<span class=\"p\">}</span>\n</code></pre></div>\n<p>The <code>&amp;Self</code> is equivalent to <code>some_struct*</code> or <code>struct Wrapper(&lt;some pointer&gt;)</code> and can be passed to C functions taking a pointer (with a cast). Writing the methods is more ergonmic this way.</p>\n<p>I think that something like <code>struct FooRef(&lt;some_pointer&gt;)</code>is typically only useful for cases where the pointer may not be valid and has to be checked somewhere else. But usually something like this should have a <code>PhantomData</code> lifetime - becauase otherwise, you could pass it around for use anywhere, even after the pointer is invalid</p>",
        "id": 341930185,
        "sender_full_name": "Trevor Gross",
        "timestamp": 1678838527
    },
    {
        "content": "<p>But I believe a <code>Wrapper(&lt;some pointer&gt;)</code> does need to be used when you want to control drop behavior</p>",
        "id": 341930968,
        "sender_full_name": "Trevor Gross",
        "timestamp": 1678838958
    }
]