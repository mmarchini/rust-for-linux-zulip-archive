[
    {
        "content": "<p><code>tc-build</code> (\"Toolchain build scripts\") just merged the ability to build Rust toolchains:</p>\n<p><a href=\"https://github.com/ClangBuiltLinux/tc-build\">https://github.com/ClangBuiltLinux/tc-build</a></p>\n<p>So now you can just do e.g.</p>\n<div class=\"codehilite\"><pre><span></span><code>./build-llvm.py --ref llvmorg-21.1.8 &amp;&amp; ./build-rust.py --ref 1.93.1\n</code></pre></div>\n\n<p>to easily build a LLVM+Rust toolchain suitable for kernel development.</p>\n<p>Or, for the upcoming Rust 1.94.0 release which supports LLVM 22 released a few days ago, you may write:</p>\n<div class=\"codehilite\"><pre><span></span><code>./build-llvm.py --ref llvmorg-22.1.0 &amp;&amp; ./build-rust.py --ref origin/beta\n</code></pre></div>\n\n<p>For context, <code>tc-build</code> scripts aim to simplify the process by handling configuration of the different build systems, by providing good defaults and by handling downloads. They also make builds faster and smaller by avoiding building pieces not needed for kernel development.</p>\n<p>The scripts come with options to customize the process. For instance, one can build LLVM toolchains with LTO, BOLT and/or PGO optimized for kernel development in particular. <code>tc-build</code> is how the very fast <a href=\"http://kernel.org\">kernel.org</a> LLVM toolchains are built!</p>\n<p>More options or features for the Rust part may be added later on, but for now the basic support is there. If something doesn't work, please let us know.</p>\n<p>We hope that helps!</p>\n<p>Thanks a lot to Nathan for supporting the original PR I sent and seeing it through.</p>",
        "id": 576509837,
        "sender_full_name": "Miguel Ojeda",
        "timestamp": 1772362511
    }
]