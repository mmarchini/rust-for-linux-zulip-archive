[
    {
        "content": "<p>Hi! Yesterday I tried writing an abstraction for the DRM GEM object layer (and the shmem helpers) and got a bit stuck on how to model it.</p>\n<p>The basic design I'm trying to wrap is:</p>\n<ul>\n<li>There is a base <code>drm_gem_object</code> struct</li>\n<li>Drivers subclass (embed) it into their own object struct</li>\n<li>The DRM subsystem uses refcounting to keep track of the lifetime of the GEM object</li>\n<li>Drivers provide a creation callback that allocates a new (driver-specific) object struct and returns a pointer to the <code>drm_gem_object</code> within</li>\n<li>Drivers provide a freeing callback that takes a <code>drm_gem_object</code>, container_of's into the driver struct, and frees any driver-specific state</li>\n<li>There are optional helper layers like <code>drm_gem_shmem</code>, so you end up with a driver object embedding a <code>drm_gem_shmem_object</code> embedding a <code>drm_gem_object</code>.</li>\n</ul>\n<p>Yesterday it was late and I didn't really get anywhere, but thinking about it again, I guess it would be something along these lines?</p>\n<ul>\n<li>Wrap <code>drm_gem_object</code> in some opaque <code>GEM::Object</code> type</li>\n<li>The driver provides a type (implementing some trait) that represents its GEM objects (<code>Foo::Object</code>)</li>\n<li>That trait provides a function that converts a reference to the object to a reference to the <code>GEM::Object</code> within (wrapped in some opaque type) (must enforce via lifetimes that the <code>GEM::Object</code> is contained?)</li>\n<li>The driver provides a callback that creates and initializes a <code>Foo::Object</code> (should this be in a <code>Box&lt;&gt;</code> already? also, the contained <code>GEM::Object</code> would be left uninitialized/zero-inited by default - how do I prevent it being used until it is initialized?)</li>\n<li>Either on return from the GEM object creation struct, or as some explicit create object function, the DRM abstraction installs the object funcs (in particular the free callback, equivalent to Drop) and calls <code>drm_gem_object_init</code>which initializes the refcounting, then leaks the memory (after putting the object in a <code>Pin&lt;Box&lt;&gt;&gt;</code> if not already?) and returns some kind of reference type that wraps the reference instead</li>\n<li>The reference type implements Clone which increases the refcount, and Drop which decreases it</li>\n<li>In the abstraction free handler, the pointer to the <code>drm_gem_object</code> passed is upgraded to a <code>Pin&lt;Box&lt;Foo::Object&gt;&gt;</code> again (how, and how do I expose this safely?) and dropped, which invokes the user's Drop handler (if any).</li>\n</ul>\n<p>And then shmem makes this more complicated:</p>\n<ul>\n<li>The shmem free function does <code>kfree</code> itself, so it assumes the user subclass struct has the shmem member at the top (but you still have to <code>kzalloc</code> yourself in the driver object create callback). I guess that means that for the Rust shmem abstraction, instead of this being a user object that contains the <code>SHMem::Object</code>, it should be a <code>SHMem::Object&lt;T&gt;</code> that takes the user fields as a generic parameter? Does it make sense to use this design for the main GEM layer too?</li>\n<li>The shmem free function also isn't installed directly as a free handler, but rather the driver's free handler is supposed to call it last. So we somehow need to go from a (now owned/boxed, since this is the free handler) reference to a <code>GEM::Object</code> from the GEM side, to whatever type the user driver provided, to its Drop implementation, and then finally the <code>SHMem::Object</code> Drop implementation which needs to be guaranteed to run last (and to run) and actually calls the shmem code to call <code>kfree</code>.</li>\n</ul>\n<p>I think I can figure out something to make it work, but there are a lot of subtleties here and I would appreciate some advice from people with more Rust experience! ^^</p>",
        "id": 296537117,
        "sender_full_name": "Asahi Lina",
        "timestamp": 1662020148
    },
    {
        "content": "<p>You may want to check out <code>rust/kernel/sync/arc.rs</code> for some inspiration. You probably don't need most of what's there, but the main commonality is that we have some struct <code>T</code> in which we need to embed a C struct: instead of doing it the C way, we define an outer struct that contains both <code>T</code> and the C struct (<code>RefInner&lt;T&gt;</code> in the <code>arc.rs</code>). </p>\n<p>So my suggestion is for you to define something like <code>gem::Object&lt;T&gt;</code>, which just holds a pointer to a private type <code>gem::ObjectInner&lt;T&gt;</code> (which itself contains a <code>T</code> and a <code>drm_gem_object</code>). You also require that <code>T</code> implement some trait, say <code>gem::ObjectOperations</code>; then you implement unsafe callbacks that use <code>container_of</code> to go from <code>drm_gem_object</code> to  <code>gem::ObjectInner&lt;T&gt;</code> and call the interface functions. It's all part of the abstraction implementation and doesn't require users to use unsafe code.</p>",
        "id": 296563293,
        "sender_full_name": "Wedson Almeida Filho",
        "timestamp": 1662030499
    },
    {
        "content": "<p>Thanks! I think I came up with something that works (I ended up calling them <code>Object&lt;T&gt;</code> and <code>ObjectRef&lt;T&gt;</code>). It got a little bit complicated since I wanted to abstract out over users using the shmem helpers and not (which leads to different implementations for <code>Object&lt;T&gt;</code>), but I think what I have should work. I need to rework the MM bindings the same way (what I have for that now is broken due to the C struct not being pinned), but once that works I'll commit and push my work-in-progress code in case anyone wants to take a look!</p>",
        "id": 296939441,
        "sender_full_name": "Asahi Lina",
        "timestamp": 1662179334
    }
]