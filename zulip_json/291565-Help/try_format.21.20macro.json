[
    {
        "content": "<p>Hi! I was thinking about implementing <code>try_format!</code> macro, the panic-free equivalent of <code>format!</code>. But the more I am looking into the code, the more questions I have and more problems I can see:</p>\n<ul>\n<li>It will require implementing a few more <code>try_*</code> functions - Are we OK with implementing new <code>try_*</code> functions inside <code>alloc</code>? Or should we generally avoid any other than 'critically required' changes there?</li>\n<li>\n<p>There is a <code>fmt::Write</code> trait implementation used inside, which uses <code>push_str</code>, which may panic. It would be consistent to create and use something like <code>TryWrite</code> trait. But I see even more problems there:<br>\n    - The <code>write_*</code> methods of the <code>Write</code> trait return <code>fmt::Result</code>. What should be returned from <code>try_write_*</code> methods? I do not like the idea of <code>Result&lt;Result&lt;..., ...&gt;, ...&gt;</code>, I do not like creating a new container error type either,<br>\n    - For the same reason, the <code>TryWrite</code> name is confusing in my opinion. All methods of <code>Write</code> already return <code>Result</code>, so they already 'try' to do something,<br>\n    - The <code>Write</code> trait is defined inside core crate. It may be inconsistent if we define <code>TryWrite</code> somewhere else.</p>\n</li>\n<li>\n<p>The format function calls <code>.expect()</code> on the <code>Result</code> returned from <code>Write</code> trait implementation. I haven't investigated if it can ever happen for <code>String</code>, but I don't very like it there.</p>\n</li>\n</ul>\n<p>What do you think about it? Should I try to implement the <code>try_format!</code>, or should we avoid such ideas?</p>",
        "id": 247809023,
        "sender_full_name": "Mateusz Jabłoński",
        "timestamp": 1627744842
    },
    {
        "content": "<p>On the topic of adding new APIs, it is fine to implement missing fallible equivalents that we need.</p>",
        "id": 247810446,
        "sender_full_name": "Miguel Ojeda",
        "timestamp": 1627746706
    },
    {
        "content": "<p>On the <code>expect()</code>, the docs for the <code>format!</code> macro (not the function) say \"This indicates an incorrect implementation since <code>fmt::Write for String</code> never returns an error itself.\"</p>",
        "id": 247811145,
        "sender_full_name": "Miguel Ojeda",
        "timestamp": 1627747106
    },
    {
        "content": "<p>(nevertheless, we should avoid the possibility of a panic)</p>",
        "id": 247811517,
        "sender_full_name": "Miguel Ojeda",
        "timestamp": 1627747245
    },
    {
        "content": "<p>Hmm, if you had your own Write mechanism, like we do in the printk impl, you can manage the allocations, right?</p>",
        "id": 247811866,
        "sender_full_name": "Alex Gaynor",
        "timestamp": 1627747430
    },
    {
        "content": "<p>Ok, thanks! I will look into printk impl then.</p>",
        "id": 247821102,
        "sender_full_name": "Mateusz Jabłoński",
        "timestamp": 1627760569
    },
    {
        "content": "<p>Is there an upstream issue for this? It would be very good to track that, and reference the <code>try_</code> function issues.</p>",
        "id": 264070970,
        "sender_full_name": "John Ericson",
        "timestamp": 1638910875
    }
]