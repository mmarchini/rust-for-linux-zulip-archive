[
    {
        "content": "<p>Hi! I'm dealing with a lot of memory-management code for the GPU driver, and one thing that keeps coming up is what types to use for addresses of different kinds (physical, CPU virtual, GPU virtual, and possibly others). Right now I have a mix of <code>u64</code> and <code>usize</code> that doesn't make a whole lot of sense. I think the Linux kernel isn't very good at typing addresses on the C side, and I wonder if we should just define our own standard for Rust code and hide the messiness behind that.</p>\n<ul>\n<li><code>io_pgtable.h</code> uses <code>unsigned long</code> for virtual addresses, <code>phys_addr_t</code> for physical addresses, and <code>size_t</code> for page sizes and page counts.</li>\n<li><code>drm_mm.h</code> uses u64 for all VAs, sizes, and everything</li>\n<li><code>dma-map-ops.h</code> uses <code>dma_addr_t</code> for device addresses, <code>phys_addr_t</code> for physical ones, and <code>size_t</code> for sizes</li>\n<li><code>rtkit.h</code> uses <code>dma_addr_t</code> for device VAs</li>\n<li><code>io.h</code> uses <code>resource_size_t</code> for both physical addresses and sizes, which is a typedef for <code>phys_addr_t</code>, except a few functions which use <code>unsigned long</code>?</li>\n</ul>\n<p>I think the root of this is mostly the <code>phys_addr_t</code> and <code>dma_addr_t</code> types (which can be separately 32-bit/64-bit depending on the architecture). Perhaps we should just wrap those publicly in rust/kernel, and maybe add some impls to make operating on them more ergonomic? I also think we should have separate variants for addresses and sizes, to avoid type confusion (you can add an addr and a size, but not two addrs, and sizes are the same width as addresses of a given context). I think those two types are probably 64 bits on all 64-bit-pointer platforms, so it probably also makes sense to allow adding <code>usize</code> to them directly, even if the width does not match. And then there's the question of how to handle wrapping... panicking probably doesn't make sense, but forcing callers to Result-check every single op is really weird with operator overloading... maybe just turn it into a warning and keep going with the wrapped result?</p>",
        "id": 300975322,
        "sender_full_name": "Asahi Lina",
        "timestamp": 1664274051
    }
]