[
    {
        "content": "<p>I wanted to share a project I've been working on called cargo-nok (No-Kbuild)</p>\n<p><a href=\"https://github.com/ardos-os/cargo-nok\">https://github.com/ardos-os/cargo-nok</a></p>\n<p>It's an experimental tool designed to build out-of-tree Rust kernel modules while keeping Cargo fully in charge of dependency resolution, without invoking the usual external-module Kbuild flow or Makefiles. It discovers the active kernel tree, derives targets/cfgs, and handles the <code>modpost</code>/<code>objtool</code>/linking stages to produce a loadable <code>.ko</code>, and cargo-nok is implemented within a single rust binary.</p>\n<p><strong>The RA Problem:</strong> Since <code>rust-project.json</code> doesn't play well with cargo, I also built a custom Cargo proxy launcher for <code>rust-analyzer</code>. It intercepts <code>cargo metadata</code> on the fly to inject the kernel crates (<code>kernel</code>, <code>bindings</code>, <code>uapi</code>, etc.) into the workspace graph and feeds the precompiled proc-macro <code>.so</code> artifacts back to RA so macros like <code>module!</code> expand perfectly.</p>\n<p>Rust analyzer crate graph:<br>\n<img alt=\"image.png\" class=\"inline-image\" data-original-content-type=\"image/png\" data-original-dimensions=\"587x387\" data-original-src=\"/user_uploads/35136/fcTd5GuDUH_LwbXvngFiF5ab/image.png\" src=\"/user_uploads/thumbnail/35136/fcTd5GuDUH_LwbXvngFiF5ab/image.png/840x560.webp\"></p>\n<p>Currently tested on x86_64, but I also coded support for some other architectures.<br>\nEverything works just as you would expect from a no_std rust crate, it integrates with rust analyzer without rust-project.json generation, you can install crates from <a href=\"http://crates.io\">crates.io</a> such as bitflags, lazy_static and popular crates, and everything just works like a normal userspace crate, all you need to do is create a normal cargo library and set the crate type to rlib and just start coding your module. Hopefully this will reduce by orders of magnitude the barrier of entry for coding for the linux kernel.</p>\n<p>Would love to hear your thoughts!</p>",
        "id": 603055267,
        "sender_full_name": "Tiago Dinis",
        "timestamp": 1781439986
    },
    {
        "content": "<p>Of course not every crate will be compatible because some rust APIs are different since linux uses custom versions of core, alloc, compiler_builtins and other essential crates, but a lot of crates should work flawlessly.</p>",
        "id": 603056998,
        "sender_full_name": "Tiago Dinis",
        "timestamp": 1781440666
    },
    {
        "content": "<p>If you remove float support from serde (I forked serde to add a <code>floats</code> feature: <a href=\"https://github.com/coffeeispower/serde/\">https://github.com/coffeeispower/serde/</a>) , it also works, you can use it with cargo nok and even use things like postcard which integrates with serde to encode and decode structs from userspace to kernel space and viceversa. This allows removing a lot of boilerplate code to safely read parameters from userspace through syscalls like ioctl, you're not thinking in bytes anymore, you're thinking in types and structs and the encoding becomes buried inside serde which increases productivity too.</p>",
        "id": 603072393,
        "sender_full_name": "Tiago Dinis",
        "timestamp": 1781446808
    }
]